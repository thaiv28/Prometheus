<!DOCTYPE html>
<html lang='en'>
<head>
    <meta charset='UTF-8'>
    <title>Prometheus Rankings</title>
    <link rel='stylesheet' href='{{ css_file }}'>
</head>
<body>
<header>
    <h1>Prometheus Esports Rankings</h1>
</header>
<main>
    <h2>Overview</h2>
    <p>Date of last update: {{ last_update }}</p>
    <nav>
        {% for metric in metrics %}
            <a href="{{ metric.key }}.html">{{ metric.name }} Rankings</a>{% if not loop.last %} | {% endif %}
        {% endfor %}
    </nav>
    <p>Prometheus is a database of sabermetric-like stats for League of Legends esports teams and players. Explore advanced metrics below.</p>
</main>
<footer>
    <a href='index.html'>Home</a>
</footer>
</body>
</html>
